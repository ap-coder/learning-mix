version: 0.2
phases:
  install:
    commands:
      - echo xxxxxxxxxxxxxxxxxxxxxx COMMANDS xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
      - curl -s https://getcomposer.org/installer | php
      - mv  composer.phar /usr/local/bin/composer
      - php --version
      - echo xxxxxxxxxxxxxxxxxxxxxx
      - composer --version
      - echo xxxxxxxxxxxxxxxxxxxxxx
      - composer update
      - apt-get update
      - apt-get install mlocate
  build:
    commands:
      - echo xxxxxxxxxxxxxxxxxxxxxx BUILD COMMANDS xxxxxxxxxxxxxxxxxxxxxxx
      - composer --version
      - echo xxxxxxxxxxxxxxxxxxxxxx
      - echo Build started on `date`
      - echo Installing composer deps
      - cp .env.example .env
      - php artisan cache:clear
  post_build:
    commands:
      - echo xxxxxxxxxxxxxxxxxxxxxx POST BUILD COMMANDS xxxxxxxxxxxxxxxxxx
      - echo Build completed on `date`
artifacts:
  type: zip
  files:
    - '**/*'
  name: learnMix-$(date +%Y-%m-%d)